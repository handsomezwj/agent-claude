# 下一课方向（第二十二课完成，课程收尾）

> 当前进度：22 课全部完成并验证过。课程主线收官——22 课的路径已经走完。
> 这文件留着记录"还能做什么"。

## 📍 已完成的（全部 22 课）

1~16：最小 agent → 工具循环 → 护栏 → eval → 裁判 → 上下文 → 摘要 → git → 多 Agent 概念 → 流式 → MCP → RAG → 词袋向量
17：向量嵌入（embedding.py，余弦相似度，免费假门）
18：真向量模型（embedding_models.py，bge-small-zh-v1.5，语义检索，番茄搜得到西红柿）
19：多 Agent 协作实战（multi_agent.py + 16-multi-agent.py + test_multi_agent.py）——双脑版：写手/评审各带各的记忆（messages），协调器只认 `.ask()` 一个门；各记各的账（信息隔离）；优雅降级 + max_rounds 兜底。独立演示 + 测试，未接 agent。
20：提示词工程深挖（prompting.py + 17-prompting.py + test_prompting.py，26 测试）——七块八股 + 温度。纯知识 + 小实验，未接 agent。中途修了 10/16/17 课 demo 的 .env 路径 bug。
21：Agent 记忆系统（memory_store.py + 18-memory.py + test_memory.py，27 测试）——记事本落盘重启不忘，已接进 agent-claude.py（CLAUDE_USE_MEMORY 开关，默认 agent_memory.json 在 agent-claude.py 旁）。
**22：上线部署原理（19-deploy.md）——文档课，不花钱。三问定生死：怎么启动（gunicorn `app:app` + `0.0.0.0:$PORT`，Windows 本地 waitress）/ 密钥放哪（.gitignore 盖 .env + 平台环境变量面板，load_dotnet 不覆盖已有变量）/ 状态存哪（内存 dict 重启就丢，生产换数据库）。含 Render 上线流程、上线后真坑（冷启动/max_tokens 空响应/超时/健康检查）、面试速答、零成本动手任务。**

你的成品 `agent-claude.py` 现在攒了：历史记忆 / 工具循环 / 四道护栏（兜底/打转/上下文/裁判）/ 摘要压缩 / 流式 / MCP / RAG+向量检索 / 真向量模型 / 长记忆记事本。
（19、20 课独立，还没接进成品；19 课想接双脑随时说。）

## 📌 还能做的（课程之外，想做再说）

1. **真部署 resume-advisor（花钱版）**：照 `19-deploy.md` 第 4 节做，得到真 URL。
   要注册 Render + 可能绑卡 + 真按 token 计费。想部署时喊我，我带你一步步走。
2. **第十九课自然延伸**：把"写手+评审"双脑接进 `agent-claude.py`（成品还只有单脑 + 裁判打分），
   配环境变量开关 + test_real_agent.py 验证不破坏护栏。说一声就做。
3. **第十八课思考题**：真模型没有"正好 0"的相似度，检索"没有资料"时只能靠阈值挡。
   生产解法：阈值调参 + 检索后让 LLM 判断资料够不够格。
4. **扣子/Dify 平台（coze-dify-笔记.md）**：coze.cn 发布/云部署要会员，已搁置；
   Dify 自部署（Docker）、阿里云百炼/百度千帆是免费替代。

## 📌 顺带记着

- 部署如果真做：API key 只放后端 `.env` 或平台环境变量面板，绝不能进 GitHub。
- 第二十一课记事本默认在 `agent-claude.py` 旁边（`agent_memory.json`），没放进 `learn-agent/` 课程目录——
  免得把用户隐私误提交到 GitHub；`CLAUDE_MEMORY_FILE` 可换位置。
- `agent-claude.py` 里 `handle_user_turn` 的 `system=build_system_text()`（长记忆）、
  `main()` 里 `connect_memory()` + 每轮 END_TURN 后 `remember_last_turn`——21 课的三个接入口。
- `kimi-改简历-prompt.md`——喂给 Kimi/任意 AI 的改简历话术（角色 + 4 步 + 不许编造），
  求职时直接用；就是第 20 课提示词工程（三要素/输出格式）的实战复用。
